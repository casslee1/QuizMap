class Question {
  constructor(questionText, answerText, lat, lon, answerImage, dayOfWeek) {
    this.questionText = questionText;
    this.answerText = answerText;
    this.lat = lat;
    this.lon = lon;
    this.answerImage = answerImage;
    this.dayOfWeek = dayOfWeek;
  }
}

export default Question;
